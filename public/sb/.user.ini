; A Scratch project is uploaded through the proxy as one request body, and the
; stock 8M would refuse the bigger ones. PHP-FPM reads this file; hosts that run
; PHP another way ignore it harmlessly, and the limit is then whatever the
; control panel says.
post_max_size = 64M
upload_max_filesize = 64M
max_execution_time = 60
